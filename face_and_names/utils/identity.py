"""
Identity and relink helpers scaffold.
"""

from __future__ import annotations

from pathlib import Path


def relink(db_root: Path) -> list[dict]:
    """Attempt to relink images when DB Root changes."""
    raise NotImplementedError
